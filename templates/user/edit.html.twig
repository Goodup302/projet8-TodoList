{% extends 'base.html.twig' %}

{% block title %}Modifier <strong>{{ user.username }}</strong>{% endblock %}

{% block body %}
    <div class="row">
        {{ form_start(form, {'attr' : {'class': 'col-12'}}) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-success pull-right">Modifier</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
